<resources>

    <style name="Theme.ShopModule" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 锁定布局在发生以下改变时，不重置状态 -->
        <item name="android:configChanges">orientation|keyboardHidden|screenSize</item>
    </style>
</resources>